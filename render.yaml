services:
  - type: web
    name: pharma-agent
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python web_app.py"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SERPER_API_KEY
        sync: false
